diff --git a/dist/highlight/config.js b/dist/highlight/config.js
index 33c3b77ab5b8f40f438ff2cfee534ebf26a5b530..1c0ec3b3cd46fc5eb4e7baf721c5a188d3464b0c 100644
--- a/dist/highlight/config.js
+++ b/dist/highlight/config.js
@@ -1,7 +1,12 @@
 //#region src/highlight/config.ts
 /** define shared configurations for Shiki */
 function defineShikiConfig(config) {
-	let created;
+	const created = new Map();
+	function getCacheKey(options) {
+		const langAlias = options?.langAlias;
+		if (!langAlias || Object.keys(langAlias).length === 0) return "";
+		return JSON.stringify(Object.entries(langAlias).sort(([a], [b]) => a.localeCompare(b)));
+	}
 	return {
 		id: Symbol(),
 		defaultThemes: config.defaultThemes,
@@ -15,12 +20,15 @@ function defineShikiConfig(config) {
 			};
 			return out;
 		},
-		createHighlighter() {
-			if (created) return created;
-			created = config.createHighlighter();
-			if ("then" in created) created = created.then((v) => created = v);
-			return created;
-		}
+		createHighlighter(options) {
+			const key = getCacheKey(options);
+			const cached = created.get(key);
+			if (cached) return cached;
+			let next = config.createHighlighter(options);
+			if ("then" in next) next = next.then((v) => (created.set(key, v), v));
+			created.set(key, next);
+			return next;
+ 		}
 	};
 }
 //#endregion
diff --git a/dist/highlight/core/index.js b/dist/highlight/core/index.js
index 2d41354e625e8ed1527cfc61aa1dcc9e63e4067a..632a8c68ef01967d91c625d64c58f3cab08cd7fb 100644
--- a/dist/highlight/core/index.js
+++ b/dist/highlight/core/index.js
@@ -4,12 +4,13 @@ import { toJsxRuntime } from "hast-util-to-jsx-runtime";
 async function highlightHast(code, options) {
 	const { fallbackLanguage = "text", config, ...resolved } = options;
 	Object.assign(resolved, config.resolveThemes(resolved));
+	if (typeof resolved.lang === "string") resolved.lang = resolved.langAlias?.[resolved.lang] || resolved.lang;
 	let themesToLoad;
 	if ("theme" in resolved) themesToLoad = [resolved.theme];
 	else if ("themes" in resolved) themesToLoad = Object.values(resolved.themes).filter((v) => v !== void 0);
 	else throw new Error("impossible");
 	const { isSpecialLang } = await import("shiki/core");
-	const highlighter = await config.createHighlighter();
+	const highlighter = await config.createHighlighter(resolved);
 	if (!isSpecialLang(resolved.lang) && !(resolved.lang in highlighter.getBundledLanguages()) && !highlighter.getLoadedLanguages().includes(resolved.lang)) resolved.lang = fallbackLanguage;
 	await Promise.all([loadMissingTheme(highlighter, ...themesToLoad), loadMissingLanguage(highlighter, resolved.lang)]);
 	return highlighter.codeToHast(code, resolved);
@@ -47,7 +48,7 @@ async function loadMissingLanguage(highlighter, ...langs) {
 * @param options - Shiki options.
 */
 async function getHighlighter(config, options) {
-	const highlighter = await config.createHighlighter();
+	const highlighter = await config.createHighlighter(options);
 	await Promise.all([options?.langs && loadMissingLanguage(highlighter, ...options.langs), options?.themes && loadMissingTheme(highlighter, ...options.themes)]);
 	return highlighter;
 }
diff --git a/dist/highlight/index.js b/dist/highlight/index.js
index f71bdfd9af8d42b17371796d7a119b24a6d3fc95..b9e5191cfce923d87b5e4aac1503d224de11e7d0 100644
--- a/dist/highlight/index.js
+++ b/dist/highlight/index.js
@@ -30,12 +30,13 @@ const defaultThemes = { themes: {
 } };
 const configDefault = defineShikiConfig({
 	defaultThemes,
-	async createHighlighter() {
+	async createHighlighter(options) {
 		const { createHighlighter } = await import("shiki");
 		const { createJavaScriptRegexEngine } = await import("shiki/engine/javascript");
 		return createHighlighter({
 			langs: [],
 			themes: [],
+			langAlias: options?.langAlias,
 			engine: createJavaScriptRegexEngine()
 		});
 	}
@@ -43,11 +44,12 @@ const configDefault = defineShikiConfig({
 /** config using the WASM powered Regex engine */
 const configWASM = defineShikiConfig({
 	defaultThemes,
-	async createHighlighter() {
+	async createHighlighter(options) {
 		const { createHighlighter, createOnigurumaEngine } = await import("shiki");
 		return createHighlighter({
 			langs: [],
 			themes: [],
+			langAlias: options?.langAlias,
 			engine: createOnigurumaEngine(import("shiki/wasm"))
 		});
 	}
diff --git a/dist/rehype-code.core-AzcK6-sL.js b/dist/rehype-code.core-AzcK6-sL.js
index 809aae56ce05cd8077d2b83dc2d2fd6a969a80a7..5a9c0b72c1a3a637125f60c41040bbf686ef623e 100644
--- a/dist/rehype-code.core-AzcK6-sL.js
+++ b/dist/rehype-code.core-AzcK6-sL.js
@@ -197,7 +197,7 @@ function createRehypeCode(configFactory) {
 		});
 		if (options.icon !== false) transformers.push(transformerIcon(options.icon));
 		if (options.tab !== false) transformers.push(transformerTab());
-		const transformer = Promise.resolve(config.createHighlighter()).then(async (highlighter) => {
+		const transformer = Promise.resolve(config.createHighlighter(options)).then(async (highlighter) => {
 			if ("themes" in options) await highlighter.loadTheme(...Object.values(options.themes).filter(Boolean));
 			else await highlighter.loadTheme(options.theme);
 			const langs = options.langs ?? (options.lazy ? [
